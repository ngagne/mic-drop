name: prod
api:
  baseUrl: https://api.example.com
  timeoutMs: 60000
  retries: 3
credentials:
  apiKey: ${PROD_API_KEY}
  clientSecret: ${PROD_CLIENT_SECRET}
headers:
  X-Client-Name: mic-check
features:
  enableMockAuth: false
  enableVerboseLogging: false
testData:
  defaultUserId: prod-user-1
  smokeSuiteEnabled: false
